/**
 * Parameter interface for endpoints that serve pages.
 * 
 * @interface PageRequestParams
 * @member {number} page The number of page to fetch.
 */
export interface PageRequestParams {
    page: number;
};